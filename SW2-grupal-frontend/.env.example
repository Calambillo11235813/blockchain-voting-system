# URL base para conectarse al backend
# En local suele ser http://localhost:3000/api
# En producción (DigitalOcean) será tu mismo dominio pero con la ruta /api
VITE_API_BASE_URL=https://elecciones-ficct.duckdns.org/api
