import { Controller } from '@nestjs/common';

@Controller('admins')
export class AdminsController {
  /**
   * Controlador de administradores (Corte Electoral).
   * Nota: endpoints específicos se implementarán según requerimientos.
   */
}
